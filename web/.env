# Default backend for the machine-view dev server (vite.config.ts).
# This is node06's LB metrics address on the tailnet; `npm run dev` proxies
# /api and /metrics to it, so live data works with zero setup on the VPN.
# Override per shell with:  UI_PROXY_TARGET=http://<host>:<port> npm run dev
UI_PROXY_TARGET=http://100.89.187.17:8007
