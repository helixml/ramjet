interface:
  display_name: "Optimize a mini-dynamo node"
  short_description: "Plan and measure GPU serving optimization safely"
  default_prompt: "Use $optimize-mini-dynamo-node to check operational admission, analyze existing evidence, and run only permitted tuning work."
