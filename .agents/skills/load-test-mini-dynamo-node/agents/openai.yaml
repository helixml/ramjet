interface:
  display_name: "Load-test mini-dynamo Node"
  short_description: "Run safe, reproducible GPU inference load tests"
  default_prompt: "Use $load-test-mini-dynamo-node to run a guarded load test and record the result."
