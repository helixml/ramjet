# Production-shaped, explicitly enabled snapshot-companion overlay.
#
# Render and validate before use:
#   docker compose -f docker-compose.yaml \
#     -f docker-compose.snapshot-companion.yaml \
#     --profile snapshot-companion config --quiet
#
# The ordinary stack never reads this file. This overlay adds only companion
# and provisioner services: it never modifies ds4-loadbalancer, so applying it
# cannot couple its /run tmpfs state to the production serving path. The LB-side
# snapshot wiring lives in the separate docker-compose.snapshot-lb.yaml, which
# must be listed explicitly and requires the boot-time authority units under
# systemd/. See #156/#157 for why that separation exists.
#
# Snapshot routing itself remains off unless MD_SNAPSHOT_ROUTE_MODE=shadow is
# set explicitly.

x-snapshot-companion-production: &snapshot-companion-production
  profiles: [snapshot-companion]
  image: ${SNAPSHOT_COMPANION_IMAGE:-ghcr.io/helixml/mini-dynamo:companion-rust-979ff7b@sha256:5cc99e33308d4cd707c0fa77629b830870ec27d05b7381320a3d948132f476f2}
  restart: unless-stopped
  init: true
  ipc: private
  read_only: true
  privileged: false
  cap_drop: [ALL]
  security_opt:
    - no-new-privileges:true
  pids_limit: 128
  cpus: 1.0
  mem_limit: 512m
  mem_reservation: 128m
  tmpfs:
    - /tmp:rw,noexec,nosuid,nodev,size=16m,mode=0700
  environment: &snapshot-companion-environment
    MD_SNAPSHOT_COMPANION_MODE: serve
    MD_SNAPSHOT_SOCKET_PATH: /run/mini-dynamo-snapshot/companion.sock
    MD_SNAPSHOT_CLIENT_UID: "12002"
    MD_SNAPSHOT_SECRET_PATH: /run/secrets/snapshot-session
    MD_SNAPSHOT_SECRET_OWNER_UID: "0"
    MD_SNAPSHOT_DIGEST_SECRET_PATH: /run/secrets/snapshot-digest
    MD_SNAPSHOT_ATTESTATION_PATH: /run/attestation/engine.json
    MD_SNAPSHOT_MAX_CLIENTS: "2"
    MD_SNAPSHOT_TAIL_QUEUE_CAPACITY: "1024"
    MD_SNAPSHOT_TAIL_QUEUE_MAX_BYTES: "16777216"
    MD_SNAPSHOT_DEADLINE_MS: "3000"
    MD_SNAPSHOT_TAIL_IDLE_DEADLINE_MS: "30000"
    MD_SNAPSHOT_SHUTDOWN_DEADLINE_MS: "5000"
    MD_SNAPSHOT_REPLAY_LIMIT: "10000"
    MD_SNAPSHOT_REPLAY_TAIL_LIMIT: "1024"
    MD_SNAPSHOT_CONNECT_TIMEOUT_MS: "2000"
    MD_SNAPSHOT_REPLAY_TIMEOUT_MS: "300000"
    MD_SNAPSHOT_RECONNECT_MIN_MS: "250"
    MD_SNAPSHOT_RECONNECT_MAX_MS: "5000"
    MD_SNAPSHOT_ATTESTATION_REFRESH_MS: "1000"
    MD_SNAPSHOT_BLOCK_SIZE: "256"
    MD_SNAPSHOT_ATTENTION_KIND: mla
    MD_SNAPSHOT_DATA_PARALLEL_RANK: "0"
    MD_SNAPSHOT_GROUP_INDEX: "0"

x-snapshot-attestation-provisioner: &snapshot-attestation-provisioner
  profiles: [snapshot-attestation]
  image: ${SNAPSHOT_COMPANION_IMAGE:-ghcr.io/helixml/mini-dynamo:companion-rust-979ff7b@sha256:5cc99e33308d4cd707c0fa77629b830870ec27d05b7381320a3d948132f476f2}
  entrypoint: ["/mini-dynamo-attestation-provisioner"]
  restart: "no"
  user: "0:12000"
  network_mode: none
  ipc: private
  read_only: true
  privileged: false
  cap_drop: [ALL]
  security_opt:
    - no-new-privileges:true
  pids_limit: 32
  cpus: 0.25
  mem_limit: 64m
  tmpfs:
    - /tmp:rw,noexec,nosuid,nodev,size=4m,mode=0700
  environment: &snapshot-attestation-environment
    MD_SNAPSHOT_ENGINE_METADATA_PATH: /run/metadata/engine.json
    MD_SNAPSHOT_DIGEST_SECRET_PATH: /run/secrets/snapshot-digest
    MD_SNAPSHOT_ATTESTATION_PATH: /run/attestation/engine.json
    MD_SNAPSHOT_SECRET_OWNER_UID: "0"
    MD_SNAPSHOT_SECRET_GROUP_GID: "12000"
    MD_SNAPSHOT_ATTESTATION_MAX_AGE_MS: "30000"

services:
  snapshot-companion-a:
    <<: *snapshot-companion-production
    container_name: snapshot-companion-a
    user: "12001:12000"
    group_add: ["12004"]
    environment:
      <<: *snapshot-companion-environment
      MD_SNAPSHOT_COMPANION_UID: "12001"
      MD_SNAPSHOT_LIVE_ENDPOINTS: tcp://dspark-0731:5557
      MD_SNAPSHOT_REPLAY_ENDPOINTS: tcp://dspark-0731:5558
      MD_SNAPSHOT_METRICS_SOCKET_PATH: /run/mini-dynamo-metrics/metrics.sock
      MD_SNAPSHOT_METRICS_GROUP_GID: "12004"
    volumes:
      - type: bind
        source: ${SNAPSHOT_RUNTIME_DIR_A:-/run/mini-dynamo-snapshot-a.invalid}
        target: /run/mini-dynamo-snapshot
        bind:
          create_host_path: false
      - type: bind
        source: ${SNAPSHOT_METRICS_DIR_A:-/run/mini-dynamo-snapshot-metrics-a.invalid}
        target: /run/mini-dynamo-metrics
        bind:
          create_host_path: false
      - type: bind
        source: ${SNAPSHOT_SESSION_SECRET_FILE_A:-/run/secrets/mini-dynamo-snapshot-session-a.invalid}
        target: /run/secrets/snapshot-session
        read_only: true
        bind:
          create_host_path: false
      - type: bind
        source: ${SNAPSHOT_DIGEST_SECRET_FILE_A:-/run/secrets/mini-dynamo-snapshot-digest-a.invalid}
        target: /run/secrets/snapshot-digest
        read_only: true
        bind:
          create_host_path: false
      - type: bind
        source: ${SNAPSHOT_ATTESTATION_DIR_A:-/run/mini-dynamo-snapshot-attestation-a.invalid}
        target: /run/attestation
        read_only: true
        bind:
          create_host_path: false
    healthcheck:
      test: ["CMD", "/mini-dynamo-snapshot-companion", "healthcheck", "/run/mini-dynamo-snapshot/companion.sock"]
      interval: 5s
      timeout: 2s
      retries: 3
      start_period: 10s
    labels:
      org.helixml.mini-dynamo.engine: engine-a
      org.helixml.mini-dynamo.role: snapshot-companion

  snapshot-companion-b:
    <<: *snapshot-companion-production
    container_name: snapshot-companion-b
    user: "12003:12000"
    group_add: ["12005"]
    environment:
      <<: *snapshot-companion-environment
      MD_SNAPSHOT_COMPANION_UID: "12003"
      MD_SNAPSHOT_LIVE_ENDPOINTS: tcp://dspark-0731-b:5557
      MD_SNAPSHOT_REPLAY_ENDPOINTS: tcp://dspark-0731-b:5558
      MD_SNAPSHOT_METRICS_SOCKET_PATH: /run/mini-dynamo-metrics/metrics.sock
      MD_SNAPSHOT_METRICS_GROUP_GID: "12005"
    volumes:
      - type: bind
        source: ${SNAPSHOT_RUNTIME_DIR_B:-/run/mini-dynamo-snapshot-b.invalid}
        target: /run/mini-dynamo-snapshot
        bind:
          create_host_path: false
      - type: bind
        source: ${SNAPSHOT_METRICS_DIR_B:-/run/mini-dynamo-snapshot-metrics-b.invalid}
        target: /run/mini-dynamo-metrics
        bind:
          create_host_path: false
      - type: bind
        source: ${SNAPSHOT_SESSION_SECRET_FILE_B:-/run/secrets/mini-dynamo-snapshot-session-b.invalid}
        target: /run/secrets/snapshot-session
        read_only: true
        bind:
          create_host_path: false
      - type: bind
        source: ${SNAPSHOT_DIGEST_SECRET_FILE_B:-/run/secrets/mini-dynamo-snapshot-digest-b.invalid}
        target: /run/secrets/snapshot-digest
        read_only: true
        bind:
          create_host_path: false
      - type: bind
        source: ${SNAPSHOT_ATTESTATION_DIR_B:-/run/mini-dynamo-snapshot-attestation-b.invalid}
        target: /run/attestation
        read_only: true
        bind:
          create_host_path: false
    healthcheck:
      test: ["CMD", "/mini-dynamo-snapshot-companion", "healthcheck", "/run/mini-dynamo-snapshot/companion.sock"]
      interval: 5s
      timeout: 2s
      retries: 3
      start_period: 10s
    labels:
      org.helixml.mini-dynamo.engine: engine-b
      org.helixml.mini-dynamo.role: snapshot-companion

  snapshot-attestation-a:
    <<: *snapshot-attestation-provisioner
    container_name: snapshot-attestation-a
    environment:
      <<: *snapshot-attestation-environment
    volumes:
      - type: bind
        source: ${SNAPSHOT_ENGINE_METADATA_FILE_A:-/run/mini-dynamo-engine-metadata-a.invalid}
        target: /run/metadata/engine.json
        read_only: true
        bind:
          create_host_path: false
      - type: bind
        source: ${SNAPSHOT_DIGEST_SECRET_FILE_A:-/run/secrets/mini-dynamo-snapshot-digest-a.invalid}
        target: /run/secrets/snapshot-digest
        read_only: true
        bind:
          create_host_path: false
      - type: bind
        source: ${SNAPSHOT_ATTESTATION_DIR_A:-/run/mini-dynamo-snapshot-attestation-a.invalid}
        target: /run/attestation
        bind:
          create_host_path: false
    labels:
      org.helixml.mini-dynamo.engine: engine-a
      org.helixml.mini-dynamo.role: attestation-provisioner

  snapshot-attestation-b:
    <<: *snapshot-attestation-provisioner
    container_name: snapshot-attestation-b
    environment:
      <<: *snapshot-attestation-environment
    volumes:
      - type: bind
        source: ${SNAPSHOT_ENGINE_METADATA_FILE_B:-/run/mini-dynamo-engine-metadata-b.invalid}
        target: /run/metadata/engine.json
        read_only: true
        bind:
          create_host_path: false
      - type: bind
        source: ${SNAPSHOT_DIGEST_SECRET_FILE_B:-/run/secrets/mini-dynamo-snapshot-digest-b.invalid}
        target: /run/secrets/snapshot-digest
        read_only: true
        bind:
          create_host_path: false
      - type: bind
        source: ${SNAPSHOT_ATTESTATION_DIR_B:-/run/mini-dynamo-snapshot-attestation-b.invalid}
        target: /run/attestation
        bind:
          create_host_path: false
    labels:
      org.helixml.mini-dynamo.engine: engine-b
      org.helixml.mini-dynamo.role: attestation-provisioner
