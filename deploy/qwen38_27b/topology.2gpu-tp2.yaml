# GENERATED by render_topology.py -- do not edit by hand.
#
# Topology: 1 engine(s) x TP2 across 2 GPU(s).
# Regenerate with:
#   ./render_topology.py --gpus 2 --tensor-parallel 2
#
# Apply on top of the base file:
#   docker compose -f docker-compose.yaml -f topology.2gpu-tp2.yaml up -d
#
# Each GPU holds about 14.0GiB of weights, so this needs a card with at
# least roughly 20GiB to leave room for KV cache and activations.

services:
  ds4-loadbalancer:
    environment:
      MD_UPSTREAM: ${MD_UPSTREAM:-http://qwen38-e0:8000}
      MD_KV_EVENT_LIVE_ENDPOINTS: ${MD_KV_EVENT_LIVE_ENDPOINTS:-tcp://qwen38-e0:5557}
      MD_KV_EVENT_REPLAY_ENDPOINTS: ${MD_KV_EVENT_REPLAY_ENDPOINTS:-tcp://qwen38-e0:5558}
  # The base file's fixed pair is parked so a stale container cannot
  # hold GPUs while this topology starts.
  qwen38-a:
    profiles: ["base-pair-only"]
  qwen38-b:
    profiles: ["base-pair-only"]
  qwen38-e0:
    image: ${IMAGE:-voipmonitor/vllm:gilded-gnosis-v20-vllm4d006a4-b12xcd3ce19-fi1ac6942-cu132-20260810-r34}
    container_name: qwen38-e0
    entrypoint: ["/opt/venv/bin/vllm", "serve", "/workspace/model"]
    restart: unless-stopped
    ipc: host
    init: true
    shm_size: 16gb
    ulimits:
      memlock: -1
      stack: 67108864
      nofile: {soft: 1048576, hard: 1048576}
    ports:
      - "127.0.0.1:8020:8000"
    volumes:
      - ${QWEN_MODEL_DIR:-/prod/models/Qwen/Qwen3.8-27B-FP8}:/workspace/model:ro
      - ${ENGINE_CACHE_DIR:-/prod/engine-cache}:/root/.cache
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              device_ids: ["0", "1"]
              capabilities: [gpu]
    environment:
      CUDA_DEVICE_ORDER: PCI_BUS_ID
      NCCL_P2P_DISABLE: ${NCCL_P2P_DISABLE:-0}
      VLLM_API_KEY: ${VLLM_API_KEY:-qwen-local}
    command:
      - --served-model-name=${SERVED_MODEL_NAME:-qwen3.8-27b}
      - --host=0.0.0.0
      - --port=8000
      - --tensor-parallel-size=2
      - --max-model-len=${MAX_MODEL_LEN:-262144}
      - --max-num-seqs=${MAX_NUM_SEQS:-256}
      - --gpu-memory-utilization=${GPU_MEMORY_UTILIZATION:-0.90}
      - --enable-prefix-caching
      - --reasoning-parser=qwen3
      - --tool-call-parser=${TOOL_CALL_PARSER:-qwen3_xml}
      - --enable-auto-tool-choice
      - --limit-mm-per-prompt={"image":${MM_IMAGE_LIMIT:-4},"video":${MM_VIDEO_LIMIT:-1}}
      - --kv-events-config={"enable_kv_cache_events":true,"publisher":"zmq","endpoint":"tcp://*:5557","replay_endpoint":"tcp://*:5558","buffer_steps":10000,"hwm":100000,"max_queue_size":100000,"topic":""}
      - --override-generation-config={"top_p":0.95}
      - --speculative-config={"method":"mtp","num_speculative_tokens":${NUM_SPECULATIVE_TOKENS:-2}}
